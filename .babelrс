{
  "presets": [
    ["@babel/preset-env",   {
        "targets": {
          "browsers": [
            "last 2 versions",
            "ie >= 11",
            "chrome >= 75",
            "firefox >= 68",
            "safari >= 12"
          ]
        }
      }],
    "@babel/preset-react"
  ]
}